# Qwen3.8-27B 调优与避坑指南

> 环境：MacBook Pro / Apple **M5 Pro / 48GB** 统一内存 · Ollama **0.33.3** · Q5_K_M 量化
> 最后更新：2026-09-07（新增**坑 0：内嵌 MTP 投机解码默认关闭**，单项提速 ~2 倍；**MLX 原生版实测快约 2 倍且首 token 延迟从分钟级降到秒级**）
>
> 这份文档记录的是**部署成功之后**才会遇到的问题。如果你还没装好，先看 `setup-guide.md`。

---

## 一、最贵的坑（按踩坑概率排序）

| # | 坑 | 典型症状 | 一句话解法 |
|---|-----|---------|-----------|
| **0** | **内嵌 MTP 投机解码默认关闭** | 生成只有 ~11 tok/s，日志里 `draft: 0` | Modelfile 加 **`PARAMETER draft_num_predict 3`**（实测代码类 +70%） |
| 1 | `PROJECTOR` 指令已废弃 | Modelfile 报 `command must be one of ...` | 改用**第二个 `FROM` 行** |
| 2 | **num_ctx 262144 反而更慢** | GPU 5% / CPU 95%，图片请求 50s+ | 改为 **131072** |
| 3 | **num_ctx 65536 触发 400** | 图片+历史超限，报 `exceeds context size` | 改为 **131072** |
| 4 | **回复内容为空** | 模型能跑，但 `content` 字段为空字符串 | 调用方传 `num_predict ≥ 2000` 或 `think=false` |
| 5 | 模型每次都重载 | 每次调用先等 6 秒 | `OLLAMA_KEEP_ALIVE=30m` |
| 6 | **发图报 `<|video_pad|>` 400** | `/v1` 端点发图报 `No data iterator found for token: <|video_pad|>` | Modelfile **必须显式写 Qwen 多模态模板**（GGUF 不内嵌模板，Ollama 会退回裸 `{{ .Prompt }}`） |

### 坑 0（2026-09-07 新增，单项提速最大）

**现象**：模型能跑，但生成只有 13 tok/s 上下，M5 Pro 48GB 完全不该这么慢。

**根因**：Qwen3.8 训练时自带 **MTP（Multi-Token Prediction）投机解码头**，GGUF 里以
`blk.64.nextn.*` 张量保留。但 Ollama 文档对 `draft_num_predict` 的原话是：

> Separate draft models default to 4; **embedded MTP tensors require setting this parameter**.

即：**内嵌的 MTP 头默认是关的**（`=0`）。不显式开，模型自带的加速硬件就一直空转。

**自检有没有这个头**：

```bash
python3 -c "import re;print(set(m.group(0) for m in re.finditer(rb'[a-zA-Z0-9_.]{2,80}nextn[a-zA-Z0-9_.]{0,40}', open('模型.gguf','rb').read(6*1024*1024))))"
# 输出含 blk.64.nextn.* 就是有
grep -a "specu" ~/.ollama/logs/server.log | tail -3
# 未启用：specu: no / draft: 0
# 已启用：specu: - n_max=4, n_min=0 + draft acceptance = 0.6~0.85
```

**实测**（M5 Pro 48GB，num_ctx=32768，关思考，200 token）：

| draft_num_predict | decode tok/s | wall time |
|---|---|---|
但**档位要按内容类型选**，这是实测中最反直觉的一点：

| 场景 | draft=0 | draft=2 | draft=3 | draft=4 | 最优 |
|---|---|---|---|---|---|
| 代码生成 | 11.71 | — | 19.89 | **21.62** | 4（+85%） |
| 开放式写作 | 10.29 | 9.74 | **11.80** | 9.92 | 3（4 反而 −4%） |
| 列表/结构化 | 9.71 | — | — | **14.43** | 4（+49%） |

**推荐默认 3**：唯一在三场景中全为正收益的档位（代码 +70%、开放式 +15%）。
draft=4 在开放式写作上会掉速——草稿被大量拒绝，验证开销白花了。
若你的负载几乎全是代码 / 工具调用 / JSON 输出，可以调到 4 再榨 9%。
**开到 6 一定掉速**（15.12），验证开销彻底反噬。

固化后默认调用实测（代码类 prompt）：关思考 **20~22 tok/s**（原 11.5，**约 +85%**），
开思考约 18 tok/s，端到端 23.1s → 9~13s。

**为什么无损**：MTP 头先猜几个 token，主模型一次性批量验证，猜对采纳、猜错用主模型结果覆盖。
draft token 只在被确认后保留，**输出分布与逐 token 解码完全一致**，不是量化、不是近似。
接受率实测 0.61~0.84：代码/结构化输出高（0.84），开放式创意写作低（0.61）——这正是上表分化的原因。

### ✅ 实测：MLX 原生版（`qwen3.8:27b-mlx`）可用且快约一倍——这是最强杠杆

前一轮我用 GGUF 格式强行 `OLLAMA_LLM_LIBRARY=mlx` 回退 Metal，就下了"MLX 死路"的错结论。
那个回退的真正原因是 **MLX runner 不认 GGUF 格式权重**，不是 Qwen3.8 架构不被支持。

官方 `qwen3.8:27b-mlx` 标签是 **MLX 原生 safetensors/nvfp4 权重**，Ollama 0.33.3 加载时**自动走原生 MLX 引擎**
（runner 日志出现 `mlx`、默认开 MTP 且接受率 0.90），不需要任何环境变量。实测三场景 tok/s：

| 场景 | Q5_K_M+MTP(Metal) | 27b-mlx(原生 MLX) | 提速 |
|---|---|---|---|
| 代码生成 | 22.4 | **40.8** | +82% |
| 开放式写作 | 13.9 | **29.0** | +109% |
| 列表结构化 | 15.2 | **34.2** | +125% |

- 该版带 `vision/thinking/tools` 能力，功能上可替现有兜底模型。
- 质量权衡：nvfp4 是 4-bit（Q5 是 5-bit），量化位低一档；但 MLX 官方优化量化，损失可控，需实际切换感受。
- **结论**："换 MLX 版本"这条路成立且是当前最强提速杠杆；"给现有 GGUF 开 MLX 开关"才是不归路。

### 体感验证（2026-09-07，Hermes Agent 实测）：TTFT 从分钟级 → 秒级

上面都是 tok/s 数字，但真正让你"感觉快了"的是**首 token 延迟（TTFT）**。

之前 Q5 版在你的机器上（其他 agent / 浏览器 / docker 必然常开），模型加载 + 超长
prompt prefill 会触发**内存 swap**——权重被换出到 SSD，单次要跟 swap 搏斗几分钟才出
第一个字。切换到 MLX 版后：权重更紧凑（18GB vs 20.7GB）+ 原生 safetensors mmap 加载
+ 常驻热（keep_alive 续命），绕开了 swap 地狱。

**实测场景**：Hermes Agent 读取一个完整项目的「代码 + 文档」、反馈项目现状。

| 配置 | 首 token 延迟 | 体感 |
|------|-------------|------|
| Q5 + MTP（Metal） | **几分钟** 无输出 | 卡死、像挂了 |
| 27b-mlx（原生 MLX） | **3~4 秒** 出字 | 几乎即时 |

这是比 tok/s 更贴近体感的指标：**之前 Q5 的「慢」大半不是生成慢，是加载/首字被
swap 卡死**；MLX 版切走这条路径后，瓶颈才回到正常的生成速度。tok/s 只快约 2 倍，
但体感快了几十倍——根因就在这里。


---

## 二、坑 1：视觉投影的挂载语法变了

### 现象

```dockerfile
FROM /path/qwen3.8-27b-Q5.gguf
PROJECTOR /path/mmproj-F16.gguf    # ← 0.33 起失效
```
```
Error: (line 2): command must be one of "from", "license", "template",
"system", "adapter", "draft", "renderer", "parser", "parameter",
"message", or "requires"
```

### 原因

Ollama 0.33 重构了 Modelfile 解析器，`PROJECTOR` 指令被移除。
视觉投影不再单独声明，而是作为**第二个 `FROM` 层**和主模型一起打包。

### 解法

```dockerfile
FROM /Users/<你的用户名>/models/qwen3.8-27b-Q5.gguf
FROM /Users/<你的用户名>/models/mmproj-F16.gguf   # ← 视觉投影，用 FROM
```

### 怎么确认真的挂上了

```bash
ollama show qwen3.8-local
```
必须同时满足两点：
- `Capabilities` 里出现 **`vision`**
- 多出一段 `Projector`，显示 `architecture clip`、约 460M 参数

只有 `completion` 没有 `vision` = 挂失败了，图片请求会返回 500。

---

## 三、坑 2（最重要）：上下文越大反而越慢

这是本次调试中发现的**最反直觉的问题**。

### 数据对比（本机实测，2026-09-04）

| num_ctx | 运行时内存 | GPU 占比 | 图片请求耗时 | 结果 |
|---------|----------|---------|------------|------|
| 262144 | ~38 GB | **5% GPU / 95% CPU** | 50–56s | ❌ 超时 499 |
| **131072** | **~28 GB** | **100% GPU** | **~24s（含思考）** | ✅ **推荐** |
| 65536 | ~23 GB | 100% GPU | 易触发 400 | ❌ 不足 |

### 原因分析

- num_ctx 262144 → KV 缓存需要额外 ~10GB → 总内存 38GB
- 48GB 统一内存中，GPU 实际可用量不足（系统和其他进程也要用）
- 模型权重被迫卸载到 CPU → 生成速度暴跌到 ~3 tok/s
- 图片请求思考耗时 50s+，客户端超时取消

- num_ctx 131072 → 总内存 ~28GB → 远未达到 GPU 容量上限 → 模型始终驻留 GPU → 9.7 tok/s

### 解法

**默认 num_ctx 131072。** 只在纯文本超长文档分析（>100K token）时临时拉高到 262144，并接受速度代价。

```dockerfile
PARAMETER num_ctx 131072   # ← 48GB 机器最佳平衡
```

### 图片 token 消耗（修正）

一张 1024×1024 的图片编码后**仅 ~1084 tokens**（非之前误写的 13 万）。
Context 不是"预算"概念——只要 GPU 内存够用，上下文可以动态增长。
真正的问题是 **GPU 卸载**，不是 token 数本身。

400 报错的真正触发条件是「图片 + 长对话历史」总和超 num_ctx，不是单张图吃掉所有预算。

### ⚠️ 2026-09-07 修正：num_ctx 本身不影响速度

开了 MTP 之后复测 8192 / 32768 / 65536 / 131072 四档，decode 全是 **13~17 tok/s，无显著差异**。
原因：Qwen3.8 是混合架构（65 层中只有 16 层全注意力，其余 48 层是 Gated DeltaNet 线性注意力，
不产生常规 KV cache），所以 KV cache 极小，上下文长度几乎不拖慢解码。

所以本节标题「上下文越大反而越慢」只在**内存被撑爆到 swap** 时成立（262144 → 38GB → 换页 → 3 tok/s）。
**num_ctx 是容量旋钮，不是速度旋钮。** 131072 的价值是"装得下图片+长历史"，不是"跑得更快"。

同样地，上一节表里 262144 那行的"慢"，真凶是 swap，不是 context 长度本身。

---

## 四、坑 3：回复内容为空（thinking 模式）

### 现象

API 返回成功（HTTP 200），但 `choices[0].message.content` 是空字符串。

### 原因

Qwen3.8 默认开启 **thinking（推理）模式**。模型会先输出一大段思考过程，
如果调用方只传 `num_predict=60`，**额度全被思考块吃光**，最终答案一个字都没输出。

### 解法

调用时传 `options.num_predict ≥ 2000`（或 `max_tokens` ≥ 2000），
或传 `think=false` 关闭思考模式。

这不是模型坏了 —— 检查返回的 `thinking` 字段，通常能看到它其实"想"了很久。

---

## 五、坑 4：模型每次都重载

### 现象
每次调用先等 6 秒（模型加载），然后才真正开始推理。

### 原因
Ollama 默认 keep_alive=5m，5 分钟内无请求就卸载模型。

### 解法
```
OLLAMA_KEEP_ALIVE=30m
```
写入 Ollama 服务环境变量（见第六节）。

代价：常驻占用约 28GB 内存；闲置 30 分钟后自动释放。

---

## 六、坑 5：`ollama run --image` 无效

到 0.33 为止 `ollama run` 仍**不支持 `--image` 标志**，测图必须走 OpenAI 兼容端点发 base64：

```bash
python3 - <<'PY'
import base64, json, urllib.request
b64 = base64.b64encode(open('/path/to/image.png','rb').read()).decode()
p = {"model":"qwen3.8-local","messages":[{"role":"user","content":[
      {"type":"text","text":"图里写了什么？简短回答。"},
      {"type":"image_url","image_url":{"url":f"data:image/png;base64,{b64}"}}]}],
    "max_tokens":2000, "stream":False}  # ← 别太小，见坑 3
r = urllib.request.urlopen(urllib.request.Request(
    "http://localhost:11434/v1/chat/completions", data=json.dumps(p).encode(),
    headers={"Content-Type":"application/json"}), timeout=300)
print(json.loads(r.read())['choices'][0]['message']['content'])
PY
```

注意端口是 **11434**（直连 Ollama）。

---

## 七、量化档位怎么选（48GB 机器的结论）

> **2026-09-09 注**：现在用的是 nvfp4 格式（18GB，MLX 引擎），比下表所有 GGUF 都小、都快。
> `qwen3.8:27b-mlx` 是官方 MLX 原生量化，无需自己转 GGUF。

先说结论：**Q5_K_M 是 48GB 机器的甜点，不建议升到 Q8。**

| 量化 | 文件 | 常驻内存 | 速度 | 质量 |
|------|------|---------|------|------|
| Q4_K_M | 15.3 GB | 17–19 GB | ~10–11 tok/s | 高 |
| **Q5_K_M** ⭐ | **18.4 GB** | **~28 GB** | **~9.7 tok/s** | **很高** |
| Q6_K_XL | 23.6 GB | 28–30 GB | ~8–9 tok/s | 更高 |
| Q8_K_XL | 29.3 GB | 35–40 GB | ~7–8 tok/s | 最高 |

> ⚠️ 上表速度列为 2026-09-04 测得，当时**尚未开启 MTP 投机解码**。开启后各档应显著上移
> （Q5_K_M 实测 11.5 → 22 tok/s），但档位间的相对关系不变：权重越小，decode 越快。
> 这是带宽受限设备的硬规律——每生成一个 token 都要把整套权重从内存搬一遍。

**为什么不升 Q8**：Q5 已经吃到 BF16 约 95%+ 的质量，Q8 补的是最后几个百分点，
而这几点**只在硬推理 / 数学 / 复杂代码上才看得出来**。日常写作、分析、总结，
输出质量更多由「推理档位 + 提示词」决定，不是量化位宽。

换来的代价却是实打实的：多下 11GB、速度慢 20%、内存多吃 7–12GB。
**除非你常让它写大段精确代码或做数学推导，否则不划算。**

---

### 坑 7（2026-09-09 新增）：未设置 num_ctx 的模型静默截断

**现象**：`qwen3.8:27b-mlx` 能跑，但读代码库时"记不住东西"，超过几千字被截断。

**根因**：该模型 Modelfile 里**没有设置 num_ctx**，Ollama 默认用 **4096 token**
（不是模型宣称的 262K，也不是之前记录的 65536）。静默截断，无任何报错。

**解法（推荐）**：用全局环境变量，**不需要为每个模型单独创建变体**：
```bash
launchctl setenv OLLAMA_CONTEXT_LENGTH 131072
pkill -f "ollama serve" && sleep 2 && open -a Ollama
```
对所有模型、所有 agent 一次生效。

**验证**：
```bash
ollama show qwen3.8:27b-mlx | grep num_ctx
# 应输出 131072，不是 4096 或"未设置"
```

**经验教训**：
- 不要为"某个模型没有 128K"就 `ollama create` 新变体——全局变量更干净
- 未设置 num_ctx 的模型用 Ollama 默认值（通常 4096），不是模型宣称的最大上下文
- 全局变量一次设置，避免模型列表越滚越多

---

## 八、三个服务级环境变量

这三个**不在 Modelfile 里**，必须配到 Ollama 的启动环境。

| 变量 | 推荐值 | 作用 | 代价 |
|------|--------|------|------|
| `OLLAMA_KEEP_ALIVE` | `30m` | 模型驻留内存，调用秒回 | 常驻占用约 28GB；闲置 30 分钟后自动释放 |
| `OLLAMA_FLASH_ATTENTION` | `1` | 降低长上下文内存占用 | 无明显代价 |
| `OLLAMA_KV_CACHE_TYPE` | `q8_0` | KV 缓存量化，131072+ 推荐 | 极轻微质量损失，换大幅内存节省 |

### macOS 配置（LaunchAgent）

```bash
python3 - <<'PY'
import plistlib
path = '/Users/<你的用户名>/Library/LaunchAgents/com.ollama.serve.plist'
d = plistlib.load(open(path,'rb'))
env = d.setdefault('EnvironmentVariables', {})
env['OLLAMA_KEEP_ALIVE'] = '30m'
env['OLLAMA_FLASH_ATTENTION'] = '1'
env['OLLAMA_KV_CACHE_TYPE'] = 'q8_0'
plistlib.dump(d, open(path,'wb'))
PY

# 重启生效
launchctl unload ~/Library/LaunchAgents/com.ollama.serve.plist 2>/dev/null
launchctl load ~/Library/LaunchAgents/com.ollama.serve.plist
```

### Linux（systemd）

```ini
# /etc/systemd/system/ollama.service.d/override.conf
[Service]
Environment="OLLAMA_FLASH_ATTENTION=1"
Environment="OLLAMA_KV_CACHE_TYPE=q8_0"
Environment="OLLAMA_KEEP_ALIVE=30m"
```
```bash
sudo systemctl daemon-reload && sudo systemctl restart ollama
```

---

## 九、一键复现的完整配置

### Modelfile

```dockerfile
FROM /Users/<你的用户名>/models/qwen3.8-27b-Q5.gguf
FROM /Users/<你的用户名>/models/mmproj-F16.gguf

# ⚠️ 该 GGUF 不内嵌 chat_template，必须显式写，否则 /v1 发图报
# "No data iterator found for token: <|video_pad|>"
TEMPLATE """{{- if .System }}<|im_start|>system
{{ .System }}<|im_end|>
{{ end }}
{{- range .Messages }}
{{- if eq .Role "user" }}<|im_start|>user
{{ .Content }}<|im_end|>
{{ else if eq .Role "assistant" }}<|im_start|>assistant
{{ if .Content }}{{ .Content }}{{ end }}{{ if .ReasoningContent }}<think>{{ .ReasoningContent }}</think>{{ end }}<|im_end|>
{{ end }}
{{- end }}<|im_start|>assistant
{{ if .ReasoningContent }}<think>{{ .ReasoningContent }}</think>{{ end }}{{ .Response }}"""

PARAMETER num_ctx 131072

# ⚠️ 内嵌 MTP 头默认关闭，必须显式开；3 为全场景正收益档（见坑 0）
PARAMETER draft_num_predict 3

PARAMETER temperature 0.7
PARAMETER top_p 0.95
PARAMETER top_k 20
PARAMETER min_p 0.0
```

### 服务环境变量

```
OLLAMA_KEEP_ALIVE=30m
OLLAMA_FLASH_ATTENTION=1
OLLAMA_KV_CACHE_TYPE=q8_0
```

### 建模型 + 验收

```bash
ollama create qwen3.8-local -f ~/models/Modelfile
ollama show qwen3.8-local     # 必须有 vision + Projector(clip)
ollama show qwen3.8-local | grep num_ctx  # 应为 131072
```

### 预期性能（M5 Pro / 48GB）

| 指标 | 数值（2026-09-07，开 MTP 后） |
|------|------|
| 生成速度 | **~20 tok/s**（代码类，关思考）/ ~18 tok/s（开思考）/ ~12 tok/s（开放式写作） |
| 首次加载 | ~5 s（驻留期间降至 ~0） |
| 常驻内存 | ~21 GB（num_ctx=131072 时约 28 GB） |
| 图片理解（1024px 单图 + 思考） | ~17s |

> 上表速度比 09-04 版本的 9.7 tok/s 高一倍，差别就在 `draft_num_predict 4`，见坑 0。

---

## 十、排查清单

出问题按顺序过一遍：

```bash
# 1. Ollama 服务活着吗
curl -s http://localhost:11434/api/version

# 2. 模型有 vision 能力吗
ollama show qwen3.8-local | grep -A3 Capabilities

# 3. num_ctx 是多少（关键！）
ollama show qwen3.8-local | grep num_ctx   # 应为 131072，不是 65536 也不是 262144

# 3.5 MTP 投机解码开了吗（最容易被忽略的提速项）
ollama show qwen3.8-local | grep draft_num_predict   # 应为 3，0 就是没开
grep -a "specu" ~/.ollama/logs/server.log | tail -3  # 应见 n_max=3 和 draft acceptance

# 4. 模型在内存里吗
ollama ps

# 5. 图片问答是否正常工作（token 预算够吗？）
python3 -c "
import base64, json, urllib.request
b64 = base64.b64encode(open('/path/to/test.png','rb').read()).decode()
p = {'model':'qwen3.8-local','messages':[{'role':'user','content':[
  {'type':'text','text':'描述这张图。'},
  {'type':'image_url','image_url':{'url':f'data:image/png;base64,{b64}'}}]}],
  'max_tokens':2000, 'stream':False}
r = urllib.request.urlopen(urllib.request.Request(
  'http://localhost:11434/v1/chat/completions', data=json.dumps(p).encode(),
  headers={'Content-Type':'application/json'}), timeout=300)
d = json.loads(r.read())
print('content:', repr(d['choices'][0]['message']['content'][:100]))
print('thinking:', repr(d.get('thinking',''))[:80])
"
```

---

*有问题欢迎交流。配置随 Ollama 版本演进，如发现失效请反馈。*
