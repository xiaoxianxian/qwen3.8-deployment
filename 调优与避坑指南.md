# Qwen3.8-27B 调优与避坑指南

> 环境：MacBook Pro / Apple **M5 Pro / 48GB** 统一内存 · Ollama **0.33.2** · Q5_K_M 量化
> 最后更新：2026-09-04（全部结论均在本机实测验证）
>
> 这份文档记录的是**部署成功之后**才会遇到的问题。如果你还没装好，先看 `setup-guide.md`。

---

## 一、五个最贵的坑（按踩坑概率排序）

| # | 坑 | 典型症状 | 一句话解法 |
|---|-----|---------|-----------|
| 1 | `PROJECTOR` 指令已废弃 | Modelfile 报 `command must be one of ...` | 改用**第二个 `FROM` 行** |
| 2 | **num_ctx 262144 反而更慢** | GPU 5% / CPU 95%，图片请求 50s+ | 改为 **65536** |
| 3 | 图片请求 499 / Request cancelled | 多数工具超时阈值 < 50s | 走代理端口 **11435** |
| 4 | 模型每次都重载 | 每次调用先等 6 秒 | `OLLAMA_KEEP_ALIVE=30m` |
| 5 | thinking 吃光输出额度 | 模型能跑，但**回复是空的** | `max_tokens` 调到 ≥ 2000 |

---

## 二、坑 1：视觉投影的挂载语法变了

### 现象

```dockerfile
FROM /path/qwen3.8-27b-Q5.gguf
PROJECTOR /path/mmproj-F16.gguf    # ← 0.33 起失效
```
```
Error: (line 2): command must be one of "from", "license", "template",
"system", "adapter", "draft", "renderer", "parser", "parameter",
"message", or "requires"
```

### 原因

Ollama 0.33 重构了 Modelfile 解析器，`PROJECTOR` 指令被移除。
视觉投影不再单独声明，而是作为**第二个 `FROM` 层**和主模型一起打包。

### 解法

```dockerfile
FROM /Users/<你的用户名>/models/qwen3.8-27b-Q5.gguf
FROM /Users/<你的用户名>/models/mmproj-F16.gguf   # ← 视觉投影，用 FROM
```

### 怎么确认真的挂上了

```bash
ollama show qwen3.8-local
```
必须同时满足两点：
- `Capabilities` 里出现 **`vision`**
- 多出一段 `Projector`，显示 `architecture clip`、约 460M 参数

只有 `completion` 没有 `vision` = 挂失败了，图片请求会返回 500。

---

## 三、坑 2（最重要）：上下文越大反而越慢

这是本次调试中发现的**最反直觉的问题**。

### 数据对比（本机实测）

| num_ctx | 运行时内存 | GPU 占比 | 图片请求耗时 | 结果 |
|---------|----------|---------|------------|------|
| 262144 | ~38 GB | **5% GPU / 95% CPU** | 50–56s | ❌ 499 超时 |
| **65536** | **~23 GB** | **100% GPU** | **~25s（medium 推理）** | ✅ 正常 |

### 原因分析

- num_ctx 262144 → KV 缓存需要额外 ~15GB → 总内存 38GB
- 48GB 统一内存中，GPU 实际可用量不足（系统和其他进程也要用）
- 模型权重被迫卸载到 CPU → 生成速度暴跌到 <2 tok/s
- 图片请求 xhigh 思考耗时 50s+，WorkBuddy 超时取消

- num_ctx 65536 → 总内存 ~23GB → 远未达到 GPU 容量上限 → 模型始终驻留 GPU → 9.6 tok/s

### 解法

**默认 num_ctx 65536。** 只在纯文本超长文档分析时临时拉高，并接受速度代价。

```dockerfile
PARAMETER num_ctx 65536   # ← 48GB 机器最佳平衡
```

### 图片 token 消耗

一张 1024×1024 的图片编码后约 **13 万 token**。但 Context 不是"预算"概念——
只要 GPU 内存够用，上下文可以动态增长。真正的问题是 **GPU 卸载**，不是 token 数本身。

---

## 四、坑 3：图片请求 500 错误 → 智能分流代理

### 现象

带图片的请求传 xhigh 会直接报 **500 Internal Server Error**（Ollama 0.33.2 已知 bug）。
部分客户端还可能报 `Request cancelled`。

### 解法：部署分流代理

代理监听 `localhost:11435`，检测到请求含图片时自动注入 `reasoning_effort=medium`：
- 纯文字请求 → 原样透传，保留原有深度思考档位
- 图片请求 → 注入 `reasoning_effort=medium`，~13s 返回，速度最快且质量最好

> 💡 **为什么用 medium？**
> 实测 Qwen3.8 推理档位表现：
> - `low`：~23s，质量一般
> - **`medium`：~13s，速度最快且质量最好**
> - `high`：~26s，和 low 差不多但更慢
> - `xhigh`：**直接报错 500**（Ollama 0.33.2 bug）
>
> 所以 medium 不是降级，而是多模态场景下的最优选择。
> 如需更高推理质量，可在 API 中显式传 `reasoning_effort: high`，代理不拦截。

代理已通过 LaunchAgent 自动运行（`com.user.ollama-strip-proxy`）。

```bash
# 验证代理状态
curl -s http://localhost:11435/api/tags

# 查看代理日志
tail -f ~/models/ollama_strip_proxy.out.log
```

### 客户端配置

**所有工具都必须把 base_url 改为 `http://localhost:11435/v1`**（注意是 11435，不是 11434）。

代理是通用的，不绑定特定工具：Hermes、WorkBuddy、Codex、Cursor 等都可使用。

WorkBuddy 配置：`~/.workbuddy/models.json`
```json
"url": "http://localhost:11435/v1"
```

### 手动启停代理

```bash
# 停止
pkill -f ollama_strip_proxy

# 启动
nohup python3 ~/models/ollama_strip_proxy.py > ~/models/ollama_strip_proxy.out.log 2>&1 &

# 或通过 launchctl（推荐）
launchctl load ~/Library/LaunchAgents/com.user.ollama-strip-proxy.plist
launchctl unload ~/Library/LaunchAgents/com.user.ollama-strip-proxy.plist
```

---

## 五、坑 4：模型每次都重载

### 现象
每次调用先等 6 秒（模型加载），然后才真正开始推理。

### 原因
Ollama 默认 keep_alive=5m，5 分钟内无请求就卸载模型。

### 解法
```
OLLAMA_KEEP_ALIVE=30m
```
写入 Ollama 服务环境变量（见第六节）。

代价：常驻占用约 23GB 内存；闲置 30 分钟后自动释放。

---

## 六、坑 5：thinking 吃光输出额度

### 现象
API 返回成功，但 `choices[0].message.content` 是空字符串。

### 原因
Qwen3.8 默认开启 **thinking（推理）模式**，模型会先输出一大段思考过程。
如果 `max_tokens` 给小了，**额度全被思考块吃光**，最终答案一个字都没输出。

### 解法
把 `max_tokens` 调大（建议 ≥ 2000），或检查返回里的 `reasoning_content` 字段——
通常能看到它其实"想"了很久。

---

## 七、坑 6：`ollama run --image` 无效

到 0.33 为止 `ollama run` 仍**不支持 `--image` 标志**，测图必须走 OpenAI 兼容端点发 base64：

```bash
python3 - <<'PY'
import base64, json, urllib.request
b64 = base64.b64encode(open('/path/to/image.png','rb').read()).decode()
p = {"model":"qwen3.8-local","messages":[{"role":"user","content":[
      {"type":"text","text":"图里写了什么？简短回答。"},
      {"type":"image_url","image_url":{"url":f"data:image/png;base64,{b64}"}}]}],
    "max_tokens":2000}          # ← 别太小，见坑 5
r = urllib.request.urlopen(urllib.request.Request(
    "http://localhost:11435/v1/chat/completions", data=json.dumps(p).encode(),
    headers={"Content-Type":"application/json"}), timeout=300)
print(json.loads(r.read())['choices'][0]['message']['content'])
PY
```

注意端口是 **11435（代理）**。

---

## 八、量化档位怎么选（48GB 机器的结论）

先说结论：**Q5_K_M 是 48GB 机器的甜点，不建议升到 Q8。**

| 量化 | 文件 | 常驻内存 | 速度 | 质量 |
|------|------|---------|------|------|
| Q4_K_M | 15.3 GB | 17–19 GB | ~10–11 tok/s | 高 |
| **Q5_K_M** ⭐ | **18.4 GB** | **~23 GB** | **~9.6 tok/s** | **很高** |
| Q6_K_XL | 23.6 GB | 28–30 GB | ~8–9 tok/s | 更高 |
| Q8_K_XL | 29.3 GB | 35–40 GB | ~7–8 tok/s | 最高 |

**为什么不升 Q8**：Q5 已经吃到 BF16 约 95%+ 的质量，Q8 补的是最后几个百分点，
而这几点**只在硬推理 / 数学 / 复杂代码上才看得出来**。日常写作、分析、总结，
输出质量更多由「推理档位 + 提示词」决定，不是量化位宽。

换来的代价却是实打实的：多下 11GB、速度慢 20%、内存多吃 13–18GB。
**除非你常让它写大段精确代码或做数学推导，否则不划算。**

---

## 九、三个服务级环境变量

这三个**不在 Modelfile 里**，必须配到 Ollama 的启动环境。

| 变量 | 推荐值 | 作用 | 代价 |
|------|--------|------|------|
| `OLLAMA_KEEP_ALIVE` | `30m` | 模型驻留内存，调用秒回 | 常驻占用约 23GB；闲置 30 分钟后自动释放 |
| `OLLAMA_FLASH_ATTENTION` | `1` | 大幅降低长上下文内存占用 | 无明显代价 |
| `OLLAMA_KV_CACHE_TYPE` | `q8_0` | KV 缓存量化，65536+ 必备 | 极轻微质量损失，换大幅内存节省 |

### macOS 配置（LaunchAgent）

```bash
python3 - <<'PY'
import plistlib
path = '/Users/<你的用户名>/Library/LaunchAgents/com.ollama.serve.plist'
d = plistlib.load(open(path,'rb'))
env = d.setdefault('EnvironmentVariables', {})
env['OLLAMA_KEEP_ALIVE'] = '30m'
env['OLLAMA_FLASH_ATTENTION'] = '1'
env['OLLAMA_KV_CACHE_TYPE'] = 'q8_0'
plistlib.dump(d, open(path,'wb'))
PY

# 重启生效
launchctl unload ~/Library/LaunchAgents/com.ollama.serve.plist 2>/dev/null
launchctl load ~/Library/LaunchAgents/com.ollama.serve.plist
```

### Linux（systemd）

```ini
# /etc/systemd/system/ollama.service.d/override.conf
[Service]
Environment="OLLAMA_FLASH_ATTENTION=1"
Environment="OLLAMA_KV_CACHE_TYPE=q8_0"
Environment="OLLAMA_KEEP_ALIVE=30m"
```
```bash
sudo systemctl daemon-reload && sudo systemctl restart ollama
```

---

## 十、一键复现的完整配置

### Modelfile

```dockerfile
FROM /Users/<你的用户名>/models/qwen3.8-27b-Q5.gguf
FROM /Users/<你的用户名>/models/mmproj-F16.gguf
PARAMETER num_ctx 65536
PARAMETER temperature 0.7
PARAMETER top_p 0.95
PARAMETER top_k 20
PARAMETER min_p 0.0
```

### 服务环境变量

```
OLLAMA_KEEP_ALIVE=30m
OLLAMA_FLASH_ATTENTION=1
OLLAMA_KV_CACHE_TYPE=q8_0
```

### 建模型 + 验收

```bash
ollama create qwen3.8-local -f ~/models/Modelfile
ollama show qwen3.8-local     # 必须有 vision + Projector(clip)
ollama show qwen3.8-local | grep num_ctx  # 应为 65536
```

### 预期性能（M5 Pro / 48GB）

| 指标 | 数值 |
|------|------|
| 生成速度 | ~9.6 tok/s（GPU 100%） |
| 首次加载 | ~6 s（驻留期间降至 ~0） |
| 常驻内存 | ~23 GB |
| 图片理解（1024px 单图，medium 推理） | **~13s** |
| 图片理解（1024px 单图，xhigh 直连） | ❌ 直接报 500 |

---

## 十一、排查清单

出问题按顺序过一遍：

```bash
# 1. Ollama 服务活着吗
curl -s http://localhost:11434/api/version

# 2. 代理活着吗
curl -s http://localhost:11435/api/tags

# 3. 模型有 vision 能力吗
ollama show qwen3.8-local | grep -A3 Capabilities

# 4. num_ctx 是多少（关键！）
ollama show qwen3.8-local | grep num_ctx   # 应为 65536，不是 262144

# 5. 模型在内存里吗
ollama ps
```

---

*有问题欢迎交流。配置随 Ollama 版本演进，如发现失效请反馈。*

---

## 附加：推理档位选择指南

| 档位 | 耗时（图片） | 质量 | 适用场景 | 备注 |
|------|------------|------|---------|------|
| low | ~23s | 一般 | 简单图片识别 | 比 medium 慢 |
| **medium** ⭐ | **~13s** | **最好** | **多模态首选** | **速度最快，质量最好** |
| high | ~26s | 较高 | 复杂推理任务 | 比 low 更慢 |
| xhigh | ❌ | — | 纯文字（不支持图片） | **图片请求直接报错 500** |

**代理默认使用 medium。** 如需调整：
```bash
# 临时调整
VISION_EFFORT=high python3 ~/models/ollama_strip_proxy.py

# 永久修改（编辑 plist 添加 EnvironmentVariables）
vim ~/Library/LaunchAgents/com.user.ollama-strip-proxy.plist
# 添加:
# <key>VISION_EFFORT</key>
# <string>high</string>
```
